# Deployment

- **Docker Compose:** Use repo root `docker-compose.yml` for local / laptop (or Rancher Desktop).
- **Kubernetes:** Manifests (Deployment, Service, optional CronJob) to be added here for AKS and Rancher Desktop.
- **Secrets:** Store Telegram token, SMTP credentials in env or K8s Secrets; never commit.
